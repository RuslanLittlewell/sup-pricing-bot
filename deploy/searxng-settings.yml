use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "price-checker-internal-searxng"
  limiter: false
  image_proxy: false

search:
  safe_search: 0
  default_lang: "pl-PL"
  formats:
    - html
    - json

outgoing:
  request_timeout: 8.0
  max_request_timeout: 15.0
