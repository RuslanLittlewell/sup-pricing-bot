# Base URL of the deployed API's /api/admin/* routes. Leave unset to use the default
# (https://pricebot-api.littlewell-app.work) — the production API, reachable directly,
# no SSH tunnel needed. Override only if testing against a locally-run backend.
VITE_API_URL=https://pricebot-api.littlewell-app.work
